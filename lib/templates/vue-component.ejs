<template>
  <div class="<%= name %>">
    <h2>{{ message }}</h2>
  </div>
</template>

<script setup>
import { ref } from 'vue'

const message = ref('Hello from <%= name %> component!')
</script>

<style scoped>
.<%= name %> {
  padding: 20px;
  border: 1px solid #ccc;
}
</style>
